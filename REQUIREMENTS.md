# 七圣召唤卡牌版本比较&选择器

## 前置数据源

1. `static-data` 仓库（当前本地计算机 `C:\Users\guyutongxue\Documents\MyFiles\repos\static-data`，CI 环境需要指定仓库 checkout）
2. 实体依赖关系表（当前本地计算机 `C:\Users\guyutongxue\Documents\MyFiles\repos\genius-invokation\packages\data-code-analyzer\src\result.json`，CI 环境下下载 `https://play.piovium.org/api/data_code_analyzer_result`）

## 呈现结果

一个可双方向滚动的大表格，每一行为不同卡牌，每一列为不同版本（不含 beta）。

### 行列排布

列排布从左到右为 3.3.0 到 最新版本。

行排布如下：
1. 所有 shareId 存在的角色牌。
2. 所有 shareId 存在的行动牌。
3. 其他实体，按 ID 大小排序。

其中，1. 2. 列出的每张牌（下称 MASTER CARDS），该行可以在行头展开以显示其“关联项”，指在《实体依赖关系表》下该实体的直接或间接 `dependencies`（但该追溯至另一含有shareId（不含）的卡牌为止）。3. 中只列出在上述追溯过程中，《实体依赖关系表》给出但未被追溯到的实体。

每一行行头除显示名称、展开折叠按钮外，同时使用 `https://static-data.piovium.org/api/v4/image/:id` 显示图片。

该表的行头、列表头需冻结显示。

### 单元格渲染

表单元格内容使用若干横排矩形填充。具体而言：对于每张牌，首先从 `static-data` 仓库获取该牌在每个版本下的中文信息：
- 对于角色牌：生命值、充能值。
- 对于行动牌：费用、描述（description）。（读取 `action_cards.json`）
- 其他实体：描述（description），以及其技能（skills）的描述。

若该信息在相邻版本相同，则“span”为同一矩形。例如实体 A 在 3.3.0-3.4.0， 3.5.0-3.6.0 各自拥有相同描述，则渲染为：

```
+----+--------+--------+--------+--------+--
|    | v3.3.0 | v3.4.0 | v3.5.0 | v3.6.0 |
+----+--------+--------+--------+--------+--
|    | +--------------+ +---------------+ +-
| A  | |       v3.4.0 | |        v3.6.0 | |
|    | +--------------+ +---------------+ +-
+----+--------+--------+--------+--------+--
```

对于 MASTER CARDS，其关联项若有不同也切割矩形。如角色 `1101` 的关联项技能 `11012` 在 3.4.0 -> 3.5.0 发生了描述变化，则 `1101` 行对应的矩形在该处也需要断开。

每个矩形可以被选中。用户可以通过**双击**矩形来切换选中的版本，使用该矩形覆盖的版本中的最新值来表示用户选择的版本。当用户选中 MASTER CARDS 的对应版本后，重置其关联项到对应版本。关联项可以指定为不同版本；此时对应 MASTER CARD 用特殊标记显示；再次双击重置关联项到同一版本。

选中的矩形可以再次双击被取消选中。

选中的矩形应当存储在 localStorage 中。

### 信息查看

用户点击对应矩形时，在弹出 drawer panel 中，使用 `@gi-tcg/card-data-viewer`（一个已发布到 npm 的 Solid.js 组件，在 genius-invokation 仓库下面有源码）显示对应的信息。panel 可以被手动关闭。

### 导出

页面增加导出功能，将用户选中的版本导出为如下格式：

```json
{
  "<id>": "<version>"
}
```

## 其他

- 构建时读取 static-data 仓库和《实体依赖关系表》得到渲染数据，不将具体描述信息打包到前端。
- 前端使用 Solid.js，展示信息用现有 `@gi-tcg/assets-manager` 方式获取。
